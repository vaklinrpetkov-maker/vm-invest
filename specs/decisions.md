# Decisions

Running one-line log of non-obvious decisions. Read before questioning a surprising choice. Add to this file whenever you make a non-obvious call — future readers will thank you.

Format:
```
[DD.MM.YYYY] — [decision] — [one-line reason]
```

---

[20.05.2026] — Shipped the renovations template-driven pivot in 5 rounds. **R1** (schema + seed): added `ApartmentSize` enum, `Team`, `ActivityTemplate`, `RenovationActivity` models alongside the legacy `RenovationTask` (additive); `lib/renovations/constants.ts` got `APARTMENT_SIZE_LABELS` + `resolveApartmentSizeFromPropertyType` (case-insensitive match against Property.type) + `APARTMENT_SIZE_DURATION_FIELD` mapping; `scripts/seed-renovation-catalog.ts` + `renovations:seed-catalog` npm alias seed 29 activities + 7 teams idempotently (find-or-update by name; bathroom-multiplied flag set on the 5 bathroom-specific templates; outsourced rows with `peopleRequired = 0`; Монтаж санитария → Team 6). **R2** (catalog admin): `/admin/renovations/teams` + `/admin/renovations/activities` (admin-only, three fields per team + 9 per activity; inline-edit by blur + ▲▼ reorder buttons + soft-delete with reference-count warnings). `lib/renovations/{catalog-actions,catalog-queries}.ts` carry the business layer; audit taxonomy extended with `team.{created,updated,deleted}` + `activity_template.{created,updated,deleted,reordered}`. Admin nav grew a "Ремонти" group. **R3** (refactor renovation flow): `Renovation.title` + `Renovation.type` columns dropped — title is derived `Ремонт — <building>/<unit>`, type is replaced by the catalog; new `lib/renovations/chain-load.ts` is pure (no DB) so it's reusable for create + rechain; `createRenovation` accepts ticked template ids + chain-loads in one transaction with the renovation insert; per-field activity setters replaced the per-field task setters (`setRenovationActivity{Name,People,Duration,Start,End,Status}`); `addRenovationActivities` appends from a sub-modal of not-yet-loaded templates (strict one-of-each); `reorderRenovationActivities` is a sortOrder-only nudge, `rechainRenovationActivities` re-runs the chain from `plannedStartDate`. `Renovation.plannedEndDate` is now derived + cached (`MAX(activity.endDate)`), recomputed inside every write that can shift it. New create form (two-step: header + activity loader checklist with computed durations); new detail-page editor (inline-editable table + Списък/Гант tab toggle); new dedicated edit form (apartmentSize + bathroomCount intentionally locked); list table dropped Title + Тип columns + gained Размер; filters parser swapped `types` → `apartmentSizes`. **R4** (capacity overlay): `lib/renovations/capacity.ts` is one SQL pass over `RenovationActivity` (uses the `(teamId, startDate)` + `(teamId, endDate)` indexes from R1) + one `Team` query → `Map<teamId, Map<dateIso, load>>` + `Set<dateIso>` overage set; no precomputed table; computed on demand. `<GanttChart>` got `dangerDays` + `viewport` props for sibling alignment; new `<TeamCapacityStrip>` renders per-team daily-load grid below the detail Gantt; portfolio Gantt + KPI tile + "Само с превишен капацитет" list filter all consume the same capacity result. Page-level fix landed mid-R4: connection-pool timeout with `connection_limit=1` was caused by wide `Promise.all` on `/renovations` — serialized the top-level awaits, made `computeRenovationKpis` sequential, and threaded a precomputed overage set through `listRenovationsForPage` so capacity runs once per page render rather than 3 times. **R5** (cleanup): `model RenovationTask` dropped from the schema (the enum `RenovationTaskStatus` stays — reused by `RenovationActivity.status`); `canEditRenovationTask` removed from permissions; `renovation.task.*` audit-event types kept in the AuditAction union for historical readability of pre-pivot events. Final spec ~590 lines; ~30 files touched across the implementation; typecheck + lint green throughout.

[20.05.2026] — Pivoted `specs/renovations.md` from free-form tasks to a **template-driven activity model** driven by the workbook `files/Renovations Gantt Chart Activities and Resources/VM Home – дейности и време по обекти в дни-3.xlsx`. New schema: `ActivityTemplate` (admin catalog of 29 seeded activities, each with name + team + people-required + 4 duration columns for едностаен/двустаен/тристаен/четиристаен + `bathroomMultiplied` flag) + `Team` (7 seeded teams + an "outsourced" sentinel = null team, each with `totalPeople`) + `RenovationActivity` (replaces `RenovationTask`; per-renovation **snapshot** of a template — fields copied at load time, no live join). Renovation header gains `apartmentSize` (enum, resolved from `Property.type` or picked manually) + `bathroomCount` (from `Property.bathroomCount`, defaults to 1); `title` dropped (derived `Ремонт — <building>/<unit>`); `type` dropped (templates supersede it). Create modal grows a step-2 checkbox loader showing the catalog; on submit, ticked templates chain-load sequentially starting at the renovation's `plannedStartDate`, each activity's `endDate = startDate + duration - 1` (calendar days; working-day calendar deferred). The 5 bathroom-specific templates carry `bathroomMultiplied = true` — their loaded duration multiplies by `renovation.bathroomCount`. Catalog edits do **not** propagate to in-flight renovations (locked answer 14); team `totalPeople` changes **do** affect capacity displays live (capacity is computed, not snapshotted). New cross-portfolio capacity check (`lib/renovations/capacity.ts`, on-demand SQL aggregation — no precomputed table; `unstable_cache` 5-minute window on KPI rollup): for each `(date, team)` sums `peopleRequired` over non-cancelled activities of non-cancelled renovations; overage tints the detail Gantt's per-team strip + the bars + the portfolio Gantt's day-bands; a new "Превишен капацитет" KPI tile + "Само с превишен капацитет" list filter surface it elsewhere. Strict one-of-each activity per renovation (locked answer 7); bathroom multiplier handles multi-bath cases. Absence module integration **deliberately not added** (locked answer 12) — Teams have no profile-membership table (locked answer 16) so per-person absence subtraction is structurally impossible. Two new admin routes: `/admin/renovations/activities` (catalog CRUD + drag-reorder) and `/admin/renovations/teams` (name/specialty/total-people). Audit taxonomy extended with `renovation.activity.{created,updated,status_changed,deleted,reordered,rechained}`, `activity_template.{created,updated,deleted}`, `team.{created,updated,deleted}`. Spec ~590 lines; replaces the previous 290-line free-task version end-to-end. Existing code (Renovation/RenovationTask schema, queries, actions, table, Gantt component) is **not yet aligned** — the implementation pivot is a separate round (likely 4 sub-rounds: schema + seed; catalog admin; create-modal loader; detail-page activity list + capacity overlay). No code touched in this round — spec-only.

[18.05.2026] — Consolidated email building blocks into `lib/email/shared.ts` after a code review flagged duplicated `wrap`/`linkButton`/`send` helpers across `lib/email/{absence,activity-feed,invite,leads}.ts`. New exports: `wrapEmail(content)` (canonical HTML shell — system font, line-height 1.5, max-width 600px), `emailButton(href, label)` (primary-tone CTA as inline `<a>`), `escapeHtml(s)` (lifted out of activity-feed; available to the others if any future copy interpolates user-supplied strings), `sendEmail({ to, subject, text, html })` (Resend wrapper that throws on error — same semantics every per-module `send` had). Migrated all four files: `absence.ts` (6 send calls + 6 `wrap`/`linkButton` usages, plus the trailing `send` function), `activity-feed.ts` (the `sendMentionEmail` inline-Resend block + `wrap`/`linkButton`/`escapeHtml` locals), `invite.ts` (previously didn't use any helpers — rewrote the raw HTML literal to use `wrapEmail` + `emailButton` for consistency; the role-label dictionary stays local), `leads.ts` (`sendEscalationDigest`). **One intentional visual delta**: `leads.ts` previously used `max-width: 640px`; the shared shell standardises on 600px. 40px narrower on wide-screen mail clients, no functional impact (mobile clients use the viewport width anyway, and 600px is the de-facto standard for transactional email). Other shells were already 600px so nothing else changes. **Latent risk left in place (out of scope)**: `leads.ts` interpolates `args.recipientName` + `l.contactName` into HTML without escaping. The helper is now available if a future round wants to harden against `<script>`-bearing names; not changing wire behaviour here. ~70 lines removed across 4 files; one new file (~70 lines including docs). Verified: `npm run typecheck` clean, `npm run lint` clean.

[18.05.2026] — Consolidated calendar layout helpers into `lib/absence/calendar.ts` after a code review flagged duplicated week-chunking + note-span math across the shared and admin calendar surfaces. Both `app/(app)/absence/calendar/page.tsx` and `app/(app)/admin/calendar/calendar-grid.tsx` had near-identical `chunkWeeks` + `computeNoteSpans` implementations differing only in how they read the note text from each cell (shared = `noteByIso.get(cell.iso)` map lookup; admin = `cell.note` direct read). Plus `app/(app)/admin/calendar/page.tsx` had its own `mondayBasedWeekday` + `buildMonthCells` (returning `(Date | null)[]` with leading/trailing gaps) — the lib already had a private `mondayBasedWeekday` and `buildMonthGrid` (which returns `Cell[]` filled with neighbour-month days, a different shape). **New lib exports:** `chunkWeeks<T>(cells)` — generic over the cell type so both `Cell[]` and `CellInfo[]` reuse the same body. `computeNoteSpans<T>(week, getNote)` — generic with a `getNote: (cell) => string | null | undefined` accessor; each caller passes its own lookup (`(c) => noteByIso.get(c.iso)` vs `(c) => c?.note`). `NoteSpan` type exported alongside. `mondayBasedWeekday(d)` promoted from private to exported. `buildMonthCellsWithGaps(year, month)` — admin's variant with `(Date | null)[]` padding (kept distinct from `buildMonthGrid` because the two return shapes are intentionally different: filled neighbour months vs empty gap squares). **Migration:** dropped ~50 lines of duplicated local helpers across the 3 files, replaced with the typed imports. The two `computeNoteSpans` call sites now use the accessor pattern; output is byte-identical for every documented input shape. Verified: `npm run typecheck` clean, `npm run lint` clean.

[18.05.2026] — Deleted 9 unused exported helpers/constants flagged by code review. Each verified individually via `grep -r {name}` across `/app`, `/components`, `/lib`, `/scripts`, `/prisma` — every one returned only its own definition file (zero callers). Removed: `serializeInvoiceFilters` (lib/invoices/filters.ts — paired with `parseInvoiceFilters` but never used since invoices doesn't have CSV export or shareable filter links); `getProfileById` (lib/profiles/search.ts — was intended for picker prefill but consumers fetch via Prisma directly); `PROPERTY_TYPE_TOOLTIPS` (lib/properties/constants.ts — hover tooltips that never made it into the UI); `canManageBuildings` (lib/properties/permissions.ts — admin building-management page uses `requireRole("admin")` directly); `getPropertiesByIds` (lib/properties/search.ts — was for contract-edit prefill; the contract form fetches via Prisma directly); `formatSellers` (lib/properties/sellers-normalize.ts — `.join(", ")` inlined wherever needed); `progressPct` (lib/renovations/queries.ts — I extracted this myself in Renovations Round 1 but never adopted it; progress math is inlined in renovations-table.tsx + detail page); `getSupabaseBrowserClient` + the whole `lib/supabase/client.ts` file (browser-side Supabase client; the app uses server-side everywhere). Plus the matcher.ts re-export shenanigans: `lib/leads/matcher.ts` had `import { CONTACT_TYPE_SYSTEM }` + `export { CONTACT_TYPE_SYSTEM }` with a comment claiming "some modules import the constant through this file" — verified that's false, dropped both. Matcher's body uses the hardcoded string `"Електронно запитване"` at line 45 (could reference the constant directly from `@/lib/contacts/constants` if anyone cares to dedupe later, but that's a separate refactor). Imports in host files retained — `requireProfile` in profiles/search.ts + properties/search.ts still used by their primary `searchX` functions; `PropertyType` + `Role` still used by sibling exports. ~70 lines removed across 8 files + 1 file deleted entirely. `npm run typecheck` + `npm run lint` clean.

[18.05.2026] — Cleaned up the one-shot seller→sellers migration leftovers. Removed `scripts/migrate-property-sellers.ts` (27-line file that only printed a "no-op: the legacy `seller` column has been dropped" message — the actual migration ran months ago), `scripts/drop-legacy-seller-column.sql` (one-shot DDL that's already executed; re-running would error since the column is gone — a footgun in active `scripts/`), and the `sellers:migrate` npm alias in `package.json`. Git history preserves both files; `specs/decisions.md` 12.05.2026 entry already documents the full migration story end-to-end (canonical-name rules, comma-split logic, what was deleted from the schema). The schema comment on `Property.sellers` was also updated — was pointing at the deleted script; now points at the decisions log entry. `scripts/check-seller-coverage.ts` + the `sellers:check` npm alias kept — that one's still useful for spot-checking canonicalisation after rule changes. ~50 lines removed. `npm run typecheck` + `npm run lint` clean.

[18.05.2026] — Wired `deleteFile` into `deleteInvoice` so the PDF on Supabase Storage actually gets removed when an invoice is deleted. Previously the action had `TODO(Round 2): actually delete from Supabase Storage once the upload flow is wired` comments — but the upload flow has been live for a while (see `app/(app)/invoices/upload-actions.ts`'s use of `uploadFile`, with the resulting key persisted as `Invoice.storagePath`). The TODO was orphan documentation; every paid + pending invoice had a corresponding storage object that survived `deleteInvoice`. New flow: DB delete (line items cascade) → `await deleteFile(before.storagePath)` → audit-log write. Ordering deliberate per the existing comment: DB first so a Storage failure leaves an orphan file (recoverable by admin sweep) rather than a missing-PDF DB row pointing nowhere. `deleteFile` itself is non-throwing (logs + returns boolean), so a Storage hiccup never blocks the audit emission. Pattern matches the existing upload-discard cleanup at `upload-actions.ts:313`. Header comment block also updated — was stale (mentioned "Round 2" + "Round 3" plans for actions that now actually exist in `[id]/field-actions.ts` + `[id]/line-item-actions.ts`). ~6 lines net. `npm run typecheck` + `npm run lint` clean.

[18.05.2026] — Removed dead `deactivateSection` server action from `app/(app)/invoices/actions.ts` (was lines 129–140). Reviewer flagged it as never-called; verified with `grep -r deactivateSection` — only its own definition matched, zero callers across `/app`, `/components`, `/lib`, or `/scripts`. The actual admin section management has its own complete `updateInvoiceSection` action in `app/(app)/admin/invoice-sections/actions.ts` that handles the deactivation path (toggles `active: false`, emits `invoices.section.deactivated` audit event, properly transactional). Dropped `requireRole` from the `@/lib/auth/session` import alongside — was only used by the dead function. ~12 lines removed. `npm run typecheck` + `npm run lint` clean.

[18.05.2026] — Extracted shared client URL-sync hook (`lib/use-filter-url.ts`) after the same review flagged duplicated debounce + comparison + page-drop plumbing across 6 filter bars (`app/(app)/{contacts,leads,meetings,contracts,properties,tasks}/filters.tsx`). Each had hand-rolled the identical idiom: a `mounted` ref to skip the first effect fire, a 250ms `setTimeout` debounce, `current.delete("page")` + sorted-key string comparison for no-op detection, `router.replace` inside `startTransition`, and a local `paramArray(sp, key)` helper for reading CSV multi-values. **New `useDebouncedQueryReplace({ pathname, targetQs, dropFromCurrent?, debounceMs? })`** centralises the navigation machinery; consumers keep only their per-module state + `targetQs` builder. Returns `{ pending }` so the "Обновяване…" indicator each bar already had still works. `dropFromCurrent` defaults to `["page"]` (every bar resets pagination on any filter change); the param exists so callers can opt out or drop more keys without rewriting the hook. `paramArray(sp, key)` co-located in the same file — distinct from `csvParam` in `lib/url-params.ts` (which operates on the `string | string[] | undefined` shape Next gives server components, not the runtime `URLSearchParams` instance `useSearchParams()` returns). Behaviour preserved exactly across all 6 modules — no debounce-timing changes, no comparison-semantic changes, no preserve-key changes. Properties still preserves navigator-owned `?building` + `?complex` keys by including them in its `targetQs` builder (the hook never touches them; same shape as before). Tasks still has its secondary `useEffect` for resyncing local state when the tab changes externally — that's tasks-specific behaviour, not part of the URL-sync mechanism, kept inline. Net result: ~180 lines removed across 6 files; one new file (~90 lines including documentation). Verified: `npm run typecheck` clean, `npm run lint` clean. The debounce → navigate → re-render roundtrip is unchanged on every documented filter-change path.

[18.05.2026] — Extracted shared URL search-param helpers (`lib/url-params.ts`) after an external code review flagged duplicated parsers across 7 filter modules. `firstString` + `splitMulti` + `parseYmd` were copy-pasted verbatim in contacts/leads/meetings; `parseNum` + `parseTri/parseTriState` in contracts + properties; page parsing inline in all seven. Replaced with: `firstParam`, `csvParam` (CSV-only — fast path, matches the most common serializer), `repeatedOrCsvParam` (accepts both styles — used by renovations + tasks where the URL is more forgiving), `parseIsoDateParam`, `parsePageParam`, `parseNumberParam`, `parseTriStateParam`. ~70 lines removed across 7 files; one new file (~90 lines including documentation). **Two minor behaviour improvements baked in (not regressions):** (1) Page parsing now uses `Number()` consistently — `?page=5abc` falls back to 1 instead of extracting 5 (was already this way in 6 of 7 modules; tasks was the outlier with `parseInt`). (2) Tasks's parser now accepts CSV URLs (`?assignee=a,b`) alongside the repeated style its serializer emits — strict superset of previous behaviour, no path that used to work now breaks. **One type shape unified:** renovations's `plannedFrom`/`plannedTo` flipped from `string | null` to `Date | null` for consistency with the other 6 modules; `queries.ts` updated to use the Date directly (no more `new Date(...T23:59:59Z)` literal — the +24h-1ms inclusive-end-of-day adjustment lands the same window). Verified: `npm run typecheck` clean, `npm run lint` clean. The URL parser → Prisma where-builder → serializer roundtrip is unchanged for every documented URL shape.

[18.05.2026] — Renovations Phase 2 Round 4 shipped — KPI strip + Само просрочени filter toggle + vertical status-history timeline. Closes out the Renovations module's Phase 2 implementation; the module is now Impl ✅. **Overdue filter**: added `overdueOnly: boolean` to `ParsedRenovationFilters`; URL key is `?overdue=1` (single-letter `1`/`true` accepted). `buildWhere` layers the constraint on top of the user's status filter — if statuses are already restricted, intersects (filters out `done`/`cancelled` from the user's list); otherwise applies `status NOT IN (done, cancelled)`. `plannedEndDate < todayUtcMidnight` always added. UI: checkbox in the filters bar with the explainer subtitle "План. край преди днес, без Завършена / Отказана". **KPI queries**: new `computeRenovationKpis(filters)` in queries.ts returns `{ activeProjects, inProgressNow, inProgressOpenTasks, overdue, completedThisQuarter }`. Each KPI runs through `stripStatusFilter` (also strips `overdueOnly`) so the user's narrowing on status doesn't fight with the tile's own status constraint — matches the invoices KPIs pattern shipped 12.05.2026. KPI 4's "current quarter" computed via `currentQuarterRange()` (UTC-quarter boundaries: 1 Jan / 1 Apr / 1 Jul / 1 Oct); membership via `updatedAt >= quarterStart` + `status = "done"` — the `updatedAt` proxy is close-enough since renovations don't get touched after they're marked done. KPI 2's subtitle uses a second query summing `RenovationTask` rows for the in-progress renovations (skipped when there are none). **`<RenovationsKpiStrip>`** server component: 4-tile grid (2-col mobile, 4-col lg), neutral chrome by default, danger-tone tint for non-zero Просрочени. Sublabel under each tile gives context ("Оферта · Одобрена · В процес" for active, "Q2 2026" for the quarter tile, etc.). Scope sublabel under the strip flips "За цялата компания" → "За филтрите по-долу" when any filter is active, so the user knows whether the numbers are filtered. **Vertical status-history timeline**: replaced the table on `/renovations/[id]` with a proper `<ol>` + absolutely-positioned vertical rail + per-entry dots. The most recent change gets an accent-tone filled dot; older changes are neutral-bordered outlines; the creation event (fromStatus null) gets a lighter outline + a "Създаване →" label instead of a from-status badge. Note column shows on its own line, indented under the timestamp. Verification: `npm run typecheck` clean, `npm run lint` clean. Renovations module Impl flipped 🟡 → ✅. With this round done, the Phase 2 implementation is complete; spec §13's out-of-scope items (drag-to-reschedule, money tracking, predecessor links, template-driven auto-seeding, pre-handover UI) remain explicitly deferred.

[18.05.2026] — Renovations Phase 2 Round 3 shipped — Gantt views (per-task + portfolio). Read-only per spec §5.3 + §6.4; drag-to-reschedule deferred. Math helpers in new `lib/gantt.ts`: `computeViewport` (auto-fits to data range + configurable pad — 7 days default — with `extraDates` for forcing inclusion of e.g. the renovation's planned bounds), `dayPct` (returns 0..100 position clamped), `rangePct` (handles missing endpoints by rendering a small stub bar so single-date rows are visible), `computeAxisTicks` (adaptive stride: ≤30d → every 5d, ≤90d → every 14d, ≤365d → monthly, >365d → bimonthly; shows year only when viewport spans multiple years), Bulgarian month abbreviations (ян/фев/март/апр/май/юни/юли/авг/сеп/окт/ное/дек), `todayUtc` exposed separately for stub-ability. Shared `<GanttChart>` component in `components/ui/gantt-chart.tsx`: pure CSS bars positioned via inline `left: N%` + `width: N%`, two-column CSS grid (200px label + flex-1 timeline), tone-per-tier color palette mapping `BadgeTone` → bar fill class (one shade darker than the matching `<StatusBadge>` background so bars stand out from row stripes), faint tick gridlines in the timeline cells, vertical accent-tone today line with a "Днес" legend below the chart, "Без дати" panel below the chart for rows that have neither start nor end. Per-task chart on `/renovations/[id]`: each task becomes one bar tinted by task status; viewport's `extraDates` include `plannedStartDate` + `plannedEndDate` so the chart always covers project scope even when tasks don't reach the edges. Portfolio chart on `/renovations`: each renovation becomes one bar tinted by renovation status; sublabel shows building · property; row label is a link to the renovation detail; optional badge shows task progress `done/total`. Tab toggle UI uses a `bg-neutral-100 p-0.5` pill with an inner `bg-neutral-0 shadow-sm` for the active tab — consistent shape across detail + list pages. Tab state is component-local (URL persistence not valuable for a once-per-session toggle). Restructured the detail-page tasks block: section chrome (title + progress + tab buttons + "+ Добави задача" button) moved up to new `<TasksSection>`; `<RenovationTasksEditor>` lost its own section wrapper, became a chrome-less table widget with `adding` lifted as a prop owned by the parent so the create button + the inline add-row coordinate properly across the tab. List page gained `<RenovationsListView>` client wrapper that holds the tab state + renders either `<RenovationsTable>` or `<GanttChart>` from the same `vmRows`. The VM gained `plannedStartIso` / `plannedEndIso` strings so the Gantt can re-hydrate the Date without parsing the display-formatted period label. Verification: `npm run typecheck` clean, `npm run lint` clean.

[18.05.2026] — Renovations Phase 2 Round 2 shipped — inline task management on the detail page. Five new per-field server actions in `app/(app)/renovations/actions.ts`: `setRenovationTaskTitle`, `setRenovationTaskDescription`, `setRenovationTaskAssignee`, `setRenovationTaskStartDate`, `setRenovationTaskEndDate` (the two date setters share a private helper). All five route through one `loadTaskAndGate` guard so the gate logic (`canEditRenovationTask`: admin/manager + renovation manager + task assignee) is enforced exactly once. Each setter emits a `renovation.task.updated` audit row with `payload: { field, renovationId }` + `before/after: { [field]: value }` — same `*.field.updated` shape as contact / tasks / meetings inline edits. New `renovation_task` entry in `FIELD_LABELS` (event-renderers.ts) covers all six editable fields (title / description / status / assigneeId / startDate / endDate). The `renovation.task.updated` renderer now branches on `payload.field`: if present, renders a proper field-diff via `renderFieldUpdated("renovation_task", ...)`; otherwise falls back to the generic "редактира задача" summary. **UI** — new client component `app/(app)/renovations/[id]/tasks-editor.tsx`: `<RenovationTasksEditor>` renders a 6-column table (Заглавие / Изпълнител / Начало / Край / Статус / actions), each cell wired to its foundation primitive (`<InlineTextCell>` / `<InlinePersonCell>` / `<InlineDateCell>` / `<InlineStatusCell>`). Per-row delete is a `×` button with native `confirm()` ("Изтриване на задача „{title}"?"). The "+ Добави задача" affordance at the top expands into a single inline row with a title-only `<Input>` (Enter saves, Esc cancels) — minimal friction; other fields can be edited inline after the row exists. Per-row edit gate (`canEditThisTask`) is resolved server-side and carried on each `TaskRowVm` so the client doesn't repeat the rule check. The detail page (`/renovations/[id]/page.tsx`) drops the read-only task table + the unused task-status badge imports, fetches active profiles for the assignee picker, computes the per-row gate, and passes everything to the editor. Verification: `npm run typecheck` clean, `npm run lint` clean. Activity feed automatically picks up the new audit emissions thanks to the existing renovation wiring + the refined renderer.

[18.05.2026] — Renovations Phase 2 Round 1 shipped — schema + CRUD + activity-feed + cross-module wiring. Lands the bulk of the spec drafted on 15.05; rounds 2-4 (inline task management, Gantt views, KPI strip) deferred to follow-up sessions. **Schema** (3 new models + 2 new enums): `Renovation` with full identity + relations + schedule columns, soft-delete; `RenovationTask` with independent date ranges (no predecessor links per spec §3.4 + locked decision), `sortOrder BigInt` defaulted to insert timestamp for stable ordering, cascade on parent hard-delete; `RenovationStatusHistory` mirrors `PropertyStatusHistory` shape — denormalised for fast detail-page render alongside the audit-log event ledger. `RenovationStatus` enum (6 states: draft/quoted/approved/in_progress/done/cancelled with Bulgarian labels Чернова/Оферта/Одобрена/В процес/Завършена/Отказана), `RenovationTaskStatus` enum (4 states: planned/in_progress/done/cancelled). Back-relations on Profile (managedRenovations / createdRenovations / deletedRenovations / assignedRenovationTasks / createdRenovationTasks / renovationStatusEvents) + Contact (requestedRenovations) + Property (renovations). **lib layer**: constants (status + task-status maps + tones + isValid* guards), filters.ts (URL parser + serializer with CSV-style multi-value keys to match leads pattern; accepts both repeated-param + CSV styles in parse), queries.ts (listForPage with where-clause builder + Promise.all of rows + count, getById with full includes, listByProperty, listByContact, listDistinctRenovationTypes, autoStampDatesFor helper for the in_progress→actualStartDate / done→actualEndDate auto-stamp rule per spec §3.1), parse.ts (FormData parser with title/description length caps + ISO date validation + date-sanity warnings), permissions.ts (open create; admin/manager edit any; user edits only own renovations as manager; admin-only delete; tasks editable by admin/manager + renovation manager + task assignee). **actions.ts**: createRenovation (defaults managerId to creator + seeds initial status-history row in same transaction), updateRenovation (auto-stamp on status transitions + writes history row when status changes), deleteRenovation (admin-only soft-delete), setRenovationStatus (inline action; transactional with status-history + auto-stamp), task CRUD (addRenovationTask / setRenovationTaskStatus / deleteRenovationTask — Round 1 ships the actions; the detail page wires them in Round 2). All actions emit audit events (renovation.created/updated/deleted/status_changed + renovation.task.* mirrors). **Audit taxonomy**: 9 new actions added to `AuditAction` union + AUDIT_LABELS map with Bulgarian labels + tones. **Activity feed**: renovation added to `SUPPORTED_TARGET_TYPES`, targetExists / resolveTargetLabel branches added; FIELD_LABELS for renovation (10 fields); event renderers for all 9 renovation.* + renovation.task.* actions including special `renovation.task.created` / `.deleted` renderers that pull the title from payload. **UI**: list page at `/renovations` with collapsible filters bar (status / type / manager / building / period chips + free-text search), table with inline status edit + progress bar + lock badge on Created; `/renovations/new` page with property pre-fill via `?propertyId=` query param (pre-fills both Имот + Заявител from Property.owner); shared `<RenovationForm>` used by new + edit; detail page `/renovations/[id]` with header (title + type + status badges + linked property/buyer/manager pills + Редактирай + Изтрий buttons), dates panel, read-only task list with progress, status-history table, activity feed; edit page reuses the form. New `<PropertyPicker>` (single-select typeahead) added to `components/ui/` — adapted from `<PropertyMultiPicker>`. **Top-nav**: Ремонти link added between Срещи and Фактури with tooltip "Проекти по ремонти на имоти — задачи, графици, отговорници." **Cross-module wiring**: `/properties/[id]` gains a Ремонти section above the activity feed with "+ Нов ремонт" button that deep-links to `/renovations/new?propertyId={id}`; `/contacts/[id]` gains a Ремонти section above its activity feed listing renovations the contact requested. Verification: `npm run db:push` clean, `npm run typecheck` clean, `npm run lint` clean. Bulgarian copy used proper closing quote `„…"` not `„…"` to avoid react/no-unescaped-entities lint. _index.md row for renovations.md flipped Spec ✅ / Impl ⏳ → Impl 🟡.

[15.05.2026] — Activity-feed "Споменавания" (mentions about me) inbox shipped — top-nav badge + `/mentions` route. Schema gained `ActivityNoteMention.seenAt DateTime?` — null = unread (counted in the badge), non-null = the user visited their inbox since the mention was posted. Mark-as-read semantics: visiting `/mentions` batch-updates every pending mention to `seenAt = now()` in one `updateMany` — Phase 1 doesn't track per-row "I've actually opened this one"; if that distinction matters later, swap to per-click marking without a schema change. Load + mark order in `app/(app)/mentions/page.tsx` is `loadInboxMentions` FIRST (snapshots `wasUnread` from `seenAt === null` at query time), then `markInboxSeen` (updates the DB) — gets the right "what was new on this visit" highlight. Inbox query (`lib/activity-feed/inbox.ts → loadInboxMentions`): newest 50 mentions for the current user regardless of read state, with author + target resolved via `resolveTargetLabel`. Filters out mentions whose parent note was soft-deleted OR whose parent record was hard-deleted (no point showing an orphaned link). Inbox page renders as a 4-column table (Кога / От / Запис / Бележка) with unread rows highlighted in `bg-accent-50/50` + a 2px accent dot before the timestamp. Each row's body uses the shared `<NoteBody>` component so `@mention` pills render correctly in the inbox view. Top-nav badge: new `countUnreadMentions(profileId)` query (cheap — covered by the existing `(mentionedProfileId, createdAt)` index) added to the layout's `Promise.all`. Link + badge follow the same conditional-render pattern as `/leads/inbox` — appears only when count > 0, hidden otherwise. Bulgarian: nav label "Споменавания", tooltip "Бележки, в които колеги ви маркираха с @.", page subtitle "Бележки, в които колегите ви споменават с @.", empty state "Нямате споменавания. Когато колега ви маркира с @, ще се появят тук." Verification: `npm run db:push` clean (one new column added), `npm run typecheck` clean, `npm run lint` clean. Activity-feed work remaining: 2.A Renovation (lands with renovations Phase 2), inbound email reply parsing (Phase 2+), cursor-anchored autocomplete popover (UX polish), reactions / event grouping / editor history (future).

[15.05.2026] — Activity-feed mentions-retry job shipped. Cron-callable endpoint `/api/activity-feed/mentions-retry` (POST + GET — both verbs supported so Vercel Cron / curl / pg_cron all work) picks up `ActivityNoteMention` rows where `notifiedAt IS NULL` and re-tries the Resend send. Shares the existing `LEADS_INGEST_TOKEN` bearer secret with the other machine-to-machine endpoints (`/api/leads/ingest`, `/api/leads/escalation-scan`) — the env var is misnamed but rotating it for a one-cred-per-cron split has no operational value; keep one shared cron credential. Scanner lives in `lib/activity-feed/retry-mentions.ts → runMentionRetryScan({ batchSize, maxAgeDays })`. Defaults: batch 50, age cap 7 days. Returns `{ scanned, succeeded, failed, skippedTooOld, skippedNoTarget, noteIds }`. Trade-offs documented in code: **(a) age cap** — mentions older than 7 days are abandoned (counted in `skippedTooOld`); a 10-day-old "you were mentioned" email is more confusing than useful, manual escape hatch is editing the note to re-trigger; **(b) no row lock** — `SELECT FOR UPDATE` skipped because overlap probability is essentially zero at our scale (cron hourly, scan completes in seconds), adds Prisma complexity for a phantom risk; **(c) no per-row attempt counter** — relying on the age cap to terminate instead of a schema-changing column. Edge cases handled by stamping `notifiedAt` even on skip (so the row doesn't keep re-getting picked up): note was soft-deleted after the mention was created; mention turned out to be a self-mention (defence-in-depth — the action layer already filters); mentioned profile was deactivated; parent record was hard-deleted (Invoice / Contract have no soft-delete, so `resolveTargetLabel` returns null in that case). Real send failures leave `notifiedAt` null with `console.error` + a `failed` counter increment so the next scan retries. Endpoint supports `?batch=N` query param for ops debugging, clamped to [1, 200]. Verification: `npm run typecheck` clean, `npm run lint` clean. With this shipped, the activity-feed reliability promise (per `audit-log.md` §5.1 + `activity-feed.md` §8.3 — "Resend hiccup never blocks the note write; retry job picks up null rows") is fully operational. Future improvements deferred to backlog (`activity-feed.md` §15): "mentions about me" inbox, inbound email reply parsing, cursor-anchored autocomplete popover, reactions, event grouping.

[15.05.2026] — Activity-feed Phase 1.C-Invoice + Phase 1.D (Contract + Property) wired. Every Phase 1 module's detail page now has a working activity feed (notes + system events + @mentions). New write-gate helper `canWriteNote(role, targetType, targetId)` in `lib/activity-feed/queries.ts` centralises the per-module rules from spec §10: open team-wide for contact/lead/meeting/property/task/renovation; admin/manager only for invoice (matches the existing inline-edit gate in `app/(app)/invoices/[id]/field-actions.ts → assertCanEdit`); contract gates user-role writes when `status === "signed"` per `contracts.md` §9 (admins + managers can still post even on signed contracts — they're the ones who'd need to flag corrections). Gate enforced server-side in `postNote` + `editNote` and surfaces in the UI: `<ActivityFeed>` (server component) calls the gate alongside `loadFeedEntries`, replaces the composer with a muted `🔒 {reason}` panel when locked, reads continue to work. Edit gate re-checks on save, so a contract going `draft → signed` after a user wrote a note locks further edits (delete still allowed via the existing author/admin/manager rule). New target-type wirings: `SUPPORTED_TARGET_TYPES` gained `invoice`, `contract`, `property`. `targetExists` branches recognise that Invoice + Contract use hard-delete (no `deletedAt` column — verified against schema; documented in code comments), Property uses soft-delete (matches the rest of the spine). `resolveTargetLabel` gained Bulgarian friendly labels: invoice → `Фактура — {vendor} №{number}`, contract → `Договор — {title} ({buyer})`, property → `Имот — {building} · {name}`. Event renderers extended: `FIELD_LABELS` map gained 30+ entries across invoice (vendor/number/dates/amounts/status), contract (title/buyer/status/type/composition/total/signed/reminder/credit/building), and property (name/status/type/description/sellers/prices/owner). Module-specific audit-action renderers added: invoices (`uploaded/metadata.edited/status.changed/deleted`), contract (`created/updated/deleted` plus `contracts.attachment.*`), property (`created/updated/deleted/status_changed/seller_normalized`). Contract's `contract.updated` renderer is special: peeks at `before/after` for `status` to render the high-signal status diff when present, falls back to generic "редактира договора" otherwise. Property delete renderer renders the optional `reason` from payload (matches the soft-delete-with-reason pattern). Detail pages each gained a single `<ActivityFeed>` mount at the bottom — `/invoices/[id]` (after the tab content), `/contracts/[id]` (after the relations grid), `/properties/[id]` (after the relations panel). All three accept the gate's lock state automatically via the server-side `canWriteNote` call inside the component. Verification: `npm run typecheck` clean, `npm run lint` clean. Activity-feed work remaining: 2.A Renovation (lands with the renovations Phase 2 implementation), plus the future-work backlog from `activity-feed.md` §15 (mentions retry job, "mentions about me" inbox, inbound email reply parsing, cursor-anchored autocomplete popover, reactions, event grouping, editor history).

[15.05.2026] — Activity-feed Phase 1.B-mentions shipped — `@mention` autocomplete in the composer, server-side parsing, `ActivityNoteMention` row writes, immediate Resend emails, pill rendering on read. Stack: `lib/activity-feed/mentions.ts` (pure parser: regex on `@` + `\p{L}/space/hyphen`, longest-prefix-match against active-profile candidates, plus a `diffMentions` helper for edits); `lib/email/activity-feed.ts` (`sendMentionEmail` Resend wrapper, subject `Споменат от {actor} в {targetLabel}`, body block-quoted excerpt truncated at 300 chars, deep link `#note-{id}` via `mentionDeepLink`); `lib/activity-feed/queries.ts` gained `loadMentionCandidates` (active profiles, ~25 rows, no caching) + `resolveTargetLabel` (Bulgarian friendly labels per `targetType` — `contact` → fullName, `lead` → "Лийд — fullName", `meeting` → "Среща — fullName (datetime)", `task` → "Задача — title"). Server actions: `postNote` parses mentions after the row write, calls a `persistAndNotifyMentions` helper that `createMany`s the join rows (`skipDuplicates: true` for safety) then loops sending emails — stamps `notifiedAt` on success, leaves null on failure so a future retry job catches the misses, **never blocks the note write** (per-row try/catch + console.error per audit-log.md §5.1 reliability stance). `editNote` re-parses on save, diffs against the body's old mentions, deletes removed rows + emits join rows + emails for newly-added (no "untag" email — would be spam). Self-mentions: stored for completeness but never emailed. Audit payloads (`activity.note.created/edited`) now carry the live `mentionCount` / `mentionsAdded` / `mentionsRemoved` instead of stub `0`s. New server action `searchMentionCandidates(query)` powers the autocomplete: substring match (case-insensitive), capped at 8 suggestions, empty query shows the first 8 alphabetically. Composer (`<NoteComposer>`) gained `detectMentionContext` (walks left from caret to find `@`-prefixed token; rejects email-style `foo@bar` by checking that the char before `@` is whitespace or start-of-string), a popover anchored beneath the textarea (full-width — cursor-anchored is a future polish per spec §8.1), ↑/↓/Enter/Tab/Esc keyboard handlers, `onMouseDown` commit (preventDefault so the textarea blur doesn't close the popover before the click), outside-click dismissal. New presentational component `<NoteBody>` tokenises the body into text + mention spans and renders pills (accent-tone bg + text) for `@Name` patterns — cosmetic only, the server parser is authoritative. Trade-offs documented inline: pills render for any `@Name` pattern regardless of whether the name resolves to a real profile (avoids fetching the candidate list on every note render); cursor-anchored popover deferred to a future polish round. Verification: `npm run typecheck` clean, `npm run lint` clean. Remaining activity-feed work: 1.C-Invoice (admin/manager-only writes), 1.D Contract + Property (locked-record rules), 2.A Renovation (with the renovations Phase 2 implementation).

[15.05.2026] — Activity-feed Phase 1.B (system events) + Phase 1.C-partial (Lead/Meeting/Task) shipped. The mixed feed (notes + audit-log-derived system events) now renders on `/contacts/[id]`, `/leads/[id]`, `/meetings/[id]`, `/tasks/[id]`. New infrastructure: `lib/activity-feed/event-renderers.ts` — keyed by `AuditAction`, returns `{ summary, detail? }` with three rendering strategies per `_foundations/activity-feed.md` §6.1 (label-only / payload context / before-after diff). Module-specific `FIELD_LABELS` map renders inline-edit events with Bulgarian column names (e.g. `phone → Телефон`); `REDACTED_FIELDS` set masks ЕГН per audit-log.md §4.4 ("Иван Петров промени ЕГН (стойностите са скрити)"). `lib/activity-feed/hidden.ts` curates `HIDDEN_ACTIONS` — file-view events (`*.attachment.viewed`), import batches (`property.imported`, `contract.imported`, `invoices.parsed`), timer escalations, plus the feed's own `activity.note.*` emissions (the note IS the feed entry; rendering its audit emission would duplicate). `loadFeedEntries(targetType, targetId)` query merges top-level notes + non-hidden audit events into one reverse-chronological `FeedEntry[]` (discriminated union by `kind: "note" | "event"`). Sort uses raw ISO timestamps separate from the display-formatted ones. Note replies stay nested inside their parent (not merged into the top-level stream). Component split: `<ActivityFeed>` (server) fetches and renders header + composer + `<FeedList>`; `<FeedList>` (client) holds the "Само ръчни" filter toggle state and renders each entry through `<NoteItem>` or `<EventItem>`. Filter chip only appears when events exist (no chip on records with notes-only). New module wirings: `SUPPORTED_TARGET_TYPES` gained `lead`, `meeting`, `task`; matching `targetExists` branches use soft-delete checks where applicable (Lead has `deletedAt`, Task doesn't — Task uses admin-only hard delete per tasks.md §7, branch documents that). One-line addition per detail page (`<ActivityFeed targetType="..." targetId={...} viewerId={me.id} viewerRole={me.role} />`) drops the feed in. Remaining: Phase 1.C-Invoice (admin/manager-only writes need extra care), 1.D (Contract + Property — locked-record rules per `contracts.md` §9 + `properties.md` §5.2), 2.A (Renovation, lands with the renovations Phase 2 implementation), 1.B-mentions (separate concern — `@mention` autocomplete + Resend email firing). Verification: `npm run typecheck` clean, `npm run lint` clean.

[15.05.2026] — Activity-feed Phase 1.A shipped — polymorphic `ActivityNote` model + Contacts wiring. Implements §5 + §11 + §12 of `specs/_foundations/activity-feed.md`. Schema: new `ActivityNote(targetType, targetId, authorId, body, parentId?, createdAt, updatedAt, editedAt, deletedAt)` table with `(targetType, targetId, createdAt)` index for the per-record feed query + `(parentId)` for the replies fan-out + `(authorId, createdAt)` for the future "my notes" surface. `ActivityNoteMention` join table shipped now so the @mention round (Phase 1.B) doesn't need another schema migration — currently unused. `ContactNote` table dropped after migration; dev DB had 0 rows so the one-shot copy script was a no-op (then deleted along with the npm alias since the model no longer exists and TypeScript would have errored on the script). Soft-delete (`deletedAt`) replaces the legacy hard-delete + cascade — deleted notes stay in the DB so the audit trail is complete and the future "Бележката е изтрита" placeholder can render in collapsed reply threads. Shared infrastructure: `lib/activity-feed/{types,queries,actions}.ts` for types, the `loadFeedNotes` server query, target-path resolver, target-exists check, and the three server actions (`postNote` / `editNote` / `deleteNote`); `components/ui/activity-feed/{note-composer,note-item,activity-feed}.tsx` for the client+server component trio. The wrapper `<ActivityFeed>` is a server component — it fetches in-place via `loadFeedNotes` and passes formatted timestamps down to the client components so they don't re-do formatting. Audit taxonomy gained three new actions (`activity.note.created/edited/deleted`) with Bulgarian labels in `audit-labels.ts`. Polymorphic-targets trade-off documented in code comments: `targetExists()` runs an application-side check on post (instead of FK enforcement) — same approach `AuditEvent` already uses; `SUPPORTED_TARGET_TYPES` set gates which target types accept writes (currently `contact` only; per-module entries added as Phase 1.C-D land). Contacts detail page `/contacts/[id]` migrated: dropped the `notesRows` Prisma include + the `topLevel.filter` JS step + the `NoteComposer` / `NoteItem` imports; replaced with a single `<ActivityFeed targetType="contact" targetId={contact.id} ... />`. Legacy `note-actions.ts` + `note-composer.tsx` + `note-item.tsx` files retired. **Phase 1.A explicitly does NOT yet include:** `@mention` parsing + Resend email firing (mentions land in 1.B-mentions), system-event rendering from the audit log (lands in 1.B-events), per-entity wiring for Lead/Meeting/Task/Invoice/Contract/Property/Renovation (Phase 1.C → 2.A per spec §13). Verification: `npm run db:push` clean both times (add tables → drop ContactNote), migration script ran with `[migrate] nothing to do — contact_notes is empty`, `npm run typecheck` + `npm run lint` clean. Windows DLL-lock note: required stopping the dev server to run `db:generate` per CLAUDE.md.

[15.05.2026] — `specs/_foundations/activity-feed.md` drafted. Catalogues the per-record narrative surface (notes + events) that lives on every major detail page. 4 user-locked decisions shape the spec: (1) **Coverage** — all major entities (Contact, Lead, Meeting, Contract, Property, Renovation, Invoice, Task) get a feed; the shipped `ContactNote` is generalised to a polymorphic `ActivityNote(targetType, targetId)` model so one table serves all of them. One-shot migration described in §5 copies existing rows over and retires the per-entity note components. Trade-off acknowledged: no FK on `targetId` — same trade `AuditEvent` already makes. (2) **@mentions notify by immediate Resend email** — one email per new mention with note excerpt + deep-link. New `ActivityNoteMention` join table drives notifications + the future "mentions about me" inbox. Self-mentions stored but not emailed; per-user opt-out deferred unless inbox noise becomes real. `notifiedAt` column lets a retry job pick up failures. (3) **Edit/delete forever, no time window** — matches the shipped `ContactNote` behaviour, zero migration friction. Author edits own; admin/manager can delete others. Edits are flagged with `(редактирана)` + hover-timestamp; the audit log keeps `before/after` body for forensic reconstruction. (4) **Same feed, visually distinct** — manual notes and system events stream into one reverse-chronological list. Notes render as full-bleed cards; events as one-line muted entries. Filter chip `Само ръчни` hides events; default off (both shown). System events synthesised from `AuditEvent` via a `lib/activity-feed/event-renderers.ts` lookup keyed by `AuditAction`; sensitive fields (ЕГН per audit-log.md §4.4) render with masked sentinels. `HIDDEN_ACTIONS` list curates noise (file-view events, import batches, timer ticks) — admin-only `Покажи всички` reveals them. Three new audit actions added: `activity.note.created/edited/deleted` (with the standard `before/after`/`payload` shape per audit-log.md §4). Phase rollout in §13: 1.A generalisation + Contacts migration → 1.B Contacts events → 1.C Lead/Meeting/Task/Invoice → 1.D Contract/Property → 2.A Renovation (already required from day one by `renovations.md` §6). Known gaps deferred: inbound email reply parsing, mentions inbox, reactions, event grouping, markdown — all called out in §15. Index row flipped Spec ⏳ → ✅.

[15.05.2026] — `specs/_foundations/audit-log.md` drafted (catalogues already-shipped infrastructure — table, helper, ~80-action taxonomy, admin viewer). 12 sections covering: what gets logged + what doesn't (§1), the `AuditEvent` schema with both existing indexes (§2), naming taxonomy + the per-module table mapping module-prefix → owner spec (§3), payload conventions standardising the `before` / `after` / `payload` split that had drifted in shipped code (§4) — `before/after` for field diffs keyed by field name, `payload` for non-diff context (status `{ from, to }`, deletion `{ reason }`, import-batch IDs, etc.). PII redaction rules locked in (§4.4): **ЕГН is never logged in values** — emit `payload: { field: "egn", masked: true }` with `before/after: { egn: null }` sentinels; phone/email log values (admin-only read, needed for forensic flows); `auth.login.failed` keeps its `payload: { email }` because identifying the targeted account is the point. Operational notes: helper wraps in try/catch + console.error so audit failure NEVER blocks the business action — deliberate trade-off (no at-least-once delivery vs. blocking legitimate edits); upgrade path documented for the future banking-grade case. Read access pegged at admin-only via `/admin/audit` (latest 100, descending). File-access logging documented as the one place where reads ARE audited (contracts/invoices attachments via `/api/files/sign`). Retention: indefinite for Phase 1 — under 100k rows expected first year, GDPR legitimate-interest covers it. Future-work TTL job sketched at ~7 years matching Bulgarian accounting retention. Cross-references: `_foundations/activity-feed.md` (pending) handles the per-record feed read surface; module-specific history tables (PropertyStatusHistory, RenovationStatusHistory) acknowledged as deliberate denormalisation duplicating audit info for fast per-record render. Step-by-step "adding a new event" checklist in §11 — the six-step ritual every new mutation follows. Known gaps in §12: no filters/pagination/search in viewer yet, no CSV export, no retention TTL, no durability queue, no PII-scrubbed GDPR-subject-export pathway. Index row flipped Spec ⏳ → ✅.

[15.05.2026] — `specs/renovations.md` drafted (Phase 2, no code yet). 8 user-locked decisions shape the spec: (1) **Scope** — Phase 2 ships path A (post-handover, owner-requested) primarily, with the schema accommodating path B (pre-handover unit prep) via a nullable `requestedByContactId` so the future addition doesn't need a migration. Common-area work + resale refurbishment of returned units explicitly deferred — both could retrofit but neither is concrete enough to justify v1 UX. (2) **Roles** — open creation across admin/manager/user, manager-led execution. (3) **Money** — no budget / actuals / client billing in Phase 2; renovations are operationally tracked but financially silent. Cost tracking comes via the invoice-linking integration already on `invoices.md` §15 (Phase 3). (4) **Gantt** — per-task Gantt within a renovation (default) + a portfolio Gantt at `/renovations` (read-only in Phase 2; drag-to-reschedule deferred). (5) **Tasks model** — `RenovationTask` is a **separate model** from the standalone `Task` module — no shared schema, no cross-surfacing in `/tasks → Мои`. The two domains describe different work and keeping them apart avoids polluting the standalone module with project-aware filters. (6) **Dependencies** — independent date ranges only; no predecessor links / cycle detection. (7) **Templates** — type is a free-text categorisation tag, not a clonable project template. The "5 templates" wording from `context.md` becomes a future enum once the canonical names settle. (8) **Lifecycle** — 6-state renovation flow (Чернова / Оферта / Одобрена / В процес / Завършена / Отказана) with a quote→approve gate upfront even without money tracking; tasks use a simpler 4-state set (Планирана / В процес / Завършена / Отказана). No `on_hold` — explicit pragmatic call; revisit if managers miss it. Per-record `RenovationStatusHistory` table mirrors `PropertyStatusHistory` (denormalised for fast detail-page rendering; the audit log handles the event-ledger role). Audit taxonomy planned: `renovation.created/updated/field.updated/status_changed/deleted` + the `renovation.task.*` mirrors. Open follow-ups documented in §14: the 5 type names + notification cadence — neither blocks the schema or Phase 2 UI.

[15.05.2026] — `/properties` inline-edit migrated to foundation primitives. The module's bespoke `app/(app)/properties/inline-cell.tsx` (mode-switcher: text / textarea / number / select with a `readOnlyReason` slot) and the matching `updatePropertyField` switchboard server action have been retired. Every editable column now uses the canonical foundation cells: Статус → `<InlineStatusCell>` (driven by `PROPERTY_STATUS_TONES`), Тип → `<InlineStatusCell>` with neutral tones (non-colored enum per §3.2), Цена EUR + Очаквана цена → `<InlineNumberCell>` (`format="currency-eur"`, `disabled={!canWritePrices}` for role gating), Описание → `<InlineMultilineCell>`, Продавач → `<InlineTextCell>` with the new `suggestions` prop (added to the primitive this round) that backs the input with a `<datalist>` for browser-native autocomplete from existing distinct values — preserves the legacy UX without a bespoke cell. Per-field server actions live in `app/(app)/properties/field-actions.ts` (mirrors the `contacts` / `tasks` layout): `setPropertyType`, `setPropertyDescription`, `setPropertySellers` (runs `parseSellerInput` for comma-split + rule-based canonicalisation), `setPropertyPriceEur`, `setPropertyExpectedPriceEur`. Each goes through a `loadAndGate` helper that runs `canEditField` against `lib/properties/permissions.ts`, so the server enforces the same role + locked-field rules the UI renders. `setPropertyStatus` stays in `status-actions.ts` because it also writes a PropertyStatusHistory row in the same transaction. All CSV-imported / Contracts-driven / derived columns (Етаж, Вход, Чиста площ + every other area / coefficient / land / yard column, Кредит, Договор/Купувач when contract-linked, Добавен, Последна промяна) now visibly carry the `<ReadOnlyBadge>` 🔒 with a Bulgarian tooltip per §3.12. Audit-log shape unchanged — still `property.updated` + `property.status_changed`, but the inline cells now emit one entry per field with `before` / `after` containing just that field's value (mirrors the per-field shape contacts uses). `<InlineOwnerCell>` deliberately kept as a module-local primitive — it wraps `<ContactPicker>` and doesn't map onto the generic cells; not worth migrating for the marginal consistency gain. Touched: `components/ui/inline-text-cell.tsx` (new `suggestions` prop), `properties-table.tsx` (full rewrite), new `field-actions.ts`, `actions.ts` (dropped `updatePropertyField` + unused imports), retired `inline-cell.tsx`. Also flipped `_index.md` payments row from `Spec ⏳` to `Spec ✅` — the `payments.md` draft was approved earlier this session.

[15.05.2026] — Full inline-edit rollout to `/leads` + `/tasks`, reusing the foundation primitives shipped during the `/contacts` round (`<InlineTextCell>`, `<InlineDateCell>`, `<InlineStatusCell>`, `<InlinePersonCell>`, `<ReadOnlyBadge>`). `/leads` gains inline Източник editing (manual/phone selectable; email_form + email_unparsed render but are `systemOnly` so the picker hides them — Resend inbound webhook is the only writer) + 🔒 badge on Създаден. `/tasks` gains inline Заглавие + Краен срок (joining the already-shipped status + owner inline cells), plus 🔒 badges on Създадена + Завършена. New server actions follow the established split: `app/(app)/leads/source-actions.ts` (`setLeadSource`) and `app/(app)/tasks/field-actions.ts` (`setTaskTitle`, `setTaskDueDate`) sit alongside the existing form-driven `actions.ts` to keep inline-edit and full-form patterns visually distinct (same layout pattern contacts uses with `field-actions.ts`). Audit additions: `leads.source.changed` (mirrors `leads.status.changed`) and `tasks.field.updated` (mirrors `contact.field.updated` / `meetings.field.updated`). Spec sync: `ui-patterns-inline-edit.md` §3.1/§3.4/§3.7/§3.12 wiring lists already mention the new modules; `tasks.md` §6–§8 updated to reflect shipped coverage + removed the stale "not yet implemented" line. Out of scope (deliberate): `/properties` (uses an older `<InlineCell>` primitive — its own refactor PR); `/contracts` (most fields are contract-locked once signed per spec §9 — modest payoff vs. import-bound table risk); inline contact-relation editing on `/leads` (rewires history, needs UX consideration); `<InlineMultiSelect>` for `LeadRow.properties` (primitive §3.3 exists, but `properties` is a `string[]` that would need a typed picker, not a generic enum picker).

[12.05.2026] — Contracts module: Phase B (template-driven `.docx` generation) cancelled, NOT just deferred. After delivering Phase A (manual create + edit form) and confirming the team's actual workflow, the conclusion is that the in-app document generator solves a problem the team doesn't have — they have long-standing Word templates and a workflow they're comfortable with externally, and the ERP's value is in capturing the structured business data (parties, properties, money, status) + holding the resulting files. `specs/contracts.md` rewritten end-to-end (from 250+ lines to ~130) to reflect this: no template inventory, no field discovery, no preview step, no versioning, no acceptance criteria for generation. File-attachment permissions opened up to all roles (admin/manager/user can upload; admin-only delete) so sales-users can add supporting documents that surface throughout a deal's life (signed-copy scans, receipts, photos of exhibits). Existing `<FileCell>` upload affordance on `/contracts/[id]` already implemented this — verified by reading `attachment-actions.ts`, no code change needed. Updates: stale "Phase B" / "Phase A" comments in `actions.ts`, `parse.ts`, `new/page.tsx` replaced with current-state language pointing at `specs/contracts.md` §11 "Out of scope". `_index.md` flipped the contracts row from Impl=🟡 to ✅. Template + tech-spec files left in `files/contracts/` for reference; not referenced by the app.

[12.05.2026] — Contracts: "Продавач (служител)" free-text field replaced by "Консултант на сделката" — a real FK to `Profile`. New `Contract.salespersonId` (`@db.Uuid`, nullable) + `salespersonProfile Profile? @relation("ContractSalesperson")`. Back-relation on `Profile.salesContracts` so future modules ("My Work" etc.) can filter contracts by `salespersonId = me.id`. Legacy `salesperson String?` column kept — CSV-imported rows have loose free-text values (nicknames, surnames) that don't map to real users. Every new save (create + edit) writes both columns: the FK and a mirror of `profile.fullName` into the legacy string column. Mirror keeps the existing text-search filter on `/contracts` working without dual-column query logic; reads through the resolved-name pattern `salespersonProfile?.fullName ?? salesperson ?? null`. New shared primitives: `lib/profiles/search.ts` server action (typeahead across `fullName` + `email`, active profiles only) + `<UserPicker />` in `components/ui/` (single-select typeahead, mirrors `<ContactPicker>` shape, reusable by future modules). Form input shows the consultant pill with `×` clear; on save the picker round-trips a single hidden `salespersonId` input. Deactivated profiles render in italic-muted style on the table + detail page with a "Този потребител е деактивиран" tooltip — the FK is not auto-cleared when someone leaves.

[12.05.2026] — Contracts module Phase A shipped — manual create + edit form. The `+ Нов договор` button on `/contracts` no longer disabled; routes to `/contracts/new`. Detail page at `/contracts/[id]` gains a `Редактирай` button that routes to `/contracts/[id]/edit`. Shared `<ContractForm />` covers both flows (mirrors the structure used by contact + meeting forms across the app: useActionState, per-field error surfaces, server-side validation). New `<PropertyMultiPicker />` primitive in `components/ui/` — typeahead multi-select that hits `searchProperties` server action and emits repeated `propertyIds` hidden inputs for the form. Permission gate per specs/contracts.md §9: admin/manager/user can create; users cannot modify a `signed` contract; managers + admins can edit any status. Form covers title, buyer name, contact (picker), properties (multi-picker), salesperson, building, contract type, composition (А / А+Г/ПМ / А+ПМ), pre/post completion, uses-credit, total due, status, signed date, reminder date. `totalPaidEur` and `totalRemainingEur` are derived (payments module owns them); the form computes `remaining = max(0, due − paid)` on update so a manual edit to the due amount keeps the remaining in sync until the next payment write. **Phase B (template-driven .docx generation)** explicitly deferred — same round corrected `specs/contracts.md` to drop the PDF requirement (output stays `.docx`, no PDF generator dependency, simpler tooling), which makes Phase B significantly more tractable when it ships. Manager + admin can create contracts from the UI immediately; document generation is a separate flow.

[12.05.2026] — `/properties` table: `Продавач` column flipped to `defaultVisible: true`. Previously hidden by default because the spec called it "Правни metadata, not for daily browsing"; user feedback after the seller→sellers migration was that the column carries enough signal now (1,288 of 1,847 non-deleted properties have populated sellers, top values include VMInvest, Pulev Invest Group, Сердика пропърти, Хоумновейт) to justify always showing it. Existing users with saved column-visibility state in localStorage keep their preference; the default only kicks in for fresh browser sessions or after clearing the `properties:visible-columns` key. Also added `npm run sellers:check` (`scripts/check-seller-coverage.ts`) — diagnostic that prints how many properties have non-empty sellers arrays plus the top 20 canonical values, useful for spot-checking after future normalization tweaks.

[12.05.2026] — `/admin/sellers` retired. The bulk-merge screen was created Phase-1.5 to clean up the 69-variant `Продавач` mess post-CSV-import. After the seller→sellers migration shipped rule-based canonicalisation on every write (`lib/properties/sellers-normalize.ts` runs from CSV import, in-form edit, and the InlineCell save path) plus `<datalist>` autocomplete from existing distinct values, the screen's reason-to-exist disappeared — newly typed variants get auto-fixed, and the long-tail values that match no rule are cleaned by inline editing on the property table itself. Removed: `app/(app)/admin/sellers/` (page + actions + client component), the `listDistinctSellersWithCounts` query from `lib/properties/queries.ts`, the menu link in `app/(app)/user-menu.tsx`. Kept: `listDistinctSellers` (still feeds the inline-cell autocomplete suggestions) + the `property.seller_normalized` audit action (historical entries still reference it). Spec `properties.md` §15 (Phase 1.5) and `_index.md` updated to reflect retirement.

[12.05.2026] — Properties: `seller String?` → `sellers String[]` migration. Single-string column became a Postgres array so a property can carry multiple sellers (co-ownership cases like `VMInvest, Петро Инвест ООД`). Canonical-name rules in `lib/properties/sellers-normalize.ts` collapse the historical 69-variant `Продавач` mess into 5 canonical labels (`Сердика пропърти`, `Pulev Invest Group`, `Росед Пропърти ЕООД`, `Яско Про Сървиз`, `VMInvest`), applied via substring-match + first-rule-wins. Comma-separated values get split into separate array entries pre-normalization. Long-tail values that match no rule are preserved trimmed (Хоумновейт, Друго, etc.) — admin cleans those via `/admin/sellers`, which now operates on the array (row-by-row replace) instead of `updateMany` on a single column. One-off migration script (`scripts/migrate-property-sellers.ts`, npm-aliased `sellers:migrate`) populated the array from the legacy column; legacy column dropped via raw SQL after `updated=0` confirmation. Inline edit on the property table is a single text input that round-trips the array as `", "`-joined — `parseSellerInput` splits/normalises/dedupes on save, with `<datalist>` autocomplete from distinct values across all properties. Spec `properties.md` §3.1 + §7.2 updated accordingly. Touched 14 files across `lib/`, `app/`, `scripts/`, and the schema.

[12.05.2026] — Invoices module: dashboard KPIs now follow the active filters. Previously the four tiles on `/invoices` aggregated over the full table — when a manager turned on "Само мои" or filtered to a section, the list below showed a subset but the tiles still reported company totals, which was confusing. Now `page.tsx` reuses the same `where` (built by `buildInvoiceWhere`) for the KPI aggregations. Two implementation nuances: (a) the Pending and Overdue tiles strip the user's `status` filter from `where` before adding their own (`pending` / `pending` + `dueDate < today`) — otherwise filtering the list to `status=paid` would force those tiles to read 0 regardless of underlying data, which is unhelpful; (b) Tile 1 "Обща стойност" relabels from "за месеца" to "за периода" and drops the month-over-month delta when a date filter is active (the comparison has no apples-to-apples baseline). With no filters active, behavior is identical to before. Reversal of the Round 5 decision that the KPIs should be a "company-wide snapshot" — user feedback that the disconnect between filtered list and unfiltered KPIs was actively confusing.

[12.05.2026] — Invoices module Rounds 7 + 8 shipped — final v1 polish. Round 7 extended the `/invoices` filter bar to spec §6.2: fuzzy search (debounced 300ms, fans across vendor + invoice number + line item description + notes), section dropdown (existing), uploader single-select (auto-disabled when "Само мои" is on), "Само мои" toggle (existing), anomalies-only toggle, status pills (Чакаща / Платена — multi-select), invoice-date from/to range. Uploader picker is single-select rather than multi-chip per the team-size pragmatism call — the "Само мои" toggle already covers the dominant use case and a single dropdown handles the next-most-common need without UX bloat. Skipped deliberately: supplier multi-select with typeahead, due-date range, amount range, "duplicate-warning-only" toggle — promote if usage suggests they're needed. Pagination prev/next buttons wired via a server-rendered `PaginationNav` that re-serialises the active filters into the page link (keeps multi-value filters like `?status=pending&status=paid` intact across page nav). File column in the list view now renders the canonical `<FileCell />` from `_foundations/ui-patterns-files.md` — clicking the PDF icon opens the preview modal with the in-place iframe + download button, signed via the existing `/api/files/sign` route (wired in Round 3). Round 8: spec sync — added invoice canonical strings to `bg-copy.md §14` (Фактура, Доставчик, Позиция, Чакаща, Платена, Ценови сигнал, …), added invoice permissions table to `roles.md §3.5`, and flipped `_index.md` to `✅ Impl` for the invoices module. The module is now feature-complete for v1; spec §15 Phase 2 items (cross-vendor item equivalence, JPG/PNG uploads, bulk upload, fuller dashboard with charts, email digest) remain deferred.

[12.05.2026] — Invoices module Round 6 shipped — inline-editable line items on the detail page. New file `app/(app)/invoices/[id]/line-items-editor.tsx` replaces the read-only line items table from Round 3 with one inline-cell per field (`InlineTextCell` for description + unit, `InlineNumberCell` for quantity / unit price / line total / VAT rate). Same permission gate as the header fields — manager edits while pending, admin always. New server-action file `app/(app)/invoices/[id]/line-item-actions.ts` exposes `setLineItem{Description,Quantity,Unit,UnitPrice,LineTotal,VatRate}`, `addLineItem`, `deleteLineItem`. The detector re-runs on `setLineItemDescription` (changes `descriptionNormalized`, which decides what counts as a comparable prior) and on `setLineItemUnitPrice` (the value being compared) — both via `detectAnomaliesSafe` after the patch. Adding a row creates `Нова позиция / 1 / бр. / 0 € / 0 € / 20%` as a placeholder. Deleting is one-click two-step (inline confirm/cancel, no modal) — rows are easy to re-add and the audit log captures every deletion. Row deletion deliberately does **not** re-run the detector for downstream invoices that may reference the deleted row as their `priceAnomalyRefInvoiceId` baseline; those refs are raw UUID strings without an FK constraint, so they linger as soft pointers and self-correct on the next upload that triggers detection. Header fields lost their `formatQuantity` / `formatUnitPrice` helpers since the new editor uses the canonical inline-cell formatting from the design system.

[12.05.2026] — Invoices module Round 5 shipped — dashboard KPI strip on `/invoices`. Four tiles above the section cards: (1) Обща стойност за месеца with month-over-month delta, (2) Чакащи плащане count + sum, (3) Просрочени (pending + dueDate < today) — danger tone when non-zero, (4) Ценови сигнали — count of invoices with at least one flagged line item. KPI aggregations run as parallel `prisma.aggregate` queries on the full table (no per-user filter — the dashboard is a company-wide snapshot regardless of the list's "Само мои" toggle below). Embedded on `/invoices` rather than a separate `/invoices/dashboard` route per the user's original PRD wording (visual dashboards shown alongside the board/table view). Scope stays tight per their "keep it short, 3-4 vital metrics" guidance — no charts dep, no per-vendor breakdown, no per-channel stacking. Promote to a fuller analytics page when there's data volume to justify it (specs/invoices.md §11).

[12.05.2026] — Invoices module Round 4 shipped — price-anomaly detector. `lib/invoices/anomaly.ts` exposes `detectAnomaliesForInvoice(invoiceId)`: for each line item on the target invoice, finds the most recent prior `InvoiceLineItem` matching `(vendorNameNormalized, descriptionNormalized)` where the prior invoice's date falls in the past 30 days and is strictly before the current invoice's date, then sets `priceAnomalyPct` + `priceAnomalyRefInvoiceId` if current unit price >1.05× prior. Below-threshold or no-prior cases **clear** any stale flag — the detector is idempotent, so editing a unit price down un-flags a previously flagged row automatically. Wired into three call-sites: (a) `confirmInvoice` after the upload transaction commits, (b) `setVendorName` field edit (changing the vendor shifts the comparable set), (c) `setInvoiceDate` field edit (shifts the 30-day window). Each call uses the `detectAnomaliesSafe` wrapper which swallows errors and logs — the detector is informational and never blocks the user flow. No emails per the user's spec simplification; the table left-border + ⚠ icon and the price-history tab carry the signal entirely. New script `scripts/backfill-invoice-anomalies.ts` (npm-aliased `invoices:backfill-anomalies`) re-runs the detector across every invoice — useful since Round 2 invoices were uploaded without a detector. Threshold (5%) and window (30d) are constants in `lib/invoices/anomaly.ts`; promote to per-item-category settings if the flat threshold becomes too noisy (specs/invoices.md §17).

[12.05.2026] — Invoices module Round 3 shipped — detail page at `/invoices/[id]` with Преглед + История на цените tabs. Tab routing via `?tab=preview|history` searchParam. Преглед is split-screen on desktop: PDF iframe (signed URL fetched server-side per page-load, 5-min TTL) on the left, editable header fields on the right. Header fields use the existing inline primitives (`InlineTextCell`, `InlineDateCell`, `InlineNumberCell`, `InlineMultilineCell`) with `disabled` driven by the permission gate — `canEditFields = role==='admin' || (role==='manager' && status==='pending')` per spec §8. Per-field server actions in `app/(app)/invoices/[id]/field-actions.ts` enforce the same gate defensively and audit every edit with `invoices.metadata.edited`. **Line items render read-only in Round 3** — full per-cell inline-editing on the line-items table is deferred since the preview modal at upload time is the primary edit surface; corrections after save are rare and currently flow through delete-while-pending + re-upload. Money-field edits (subtotal/vat/total) enforce coherence at edit time: a single-field edit that breaks `total ≈ subtotal + vat ± 0.02 €` rejects with a message asking the user to update all three. Sign route at `/api/files/sign` learned the `invoices` module case — looks up the Invoice by id (the route uses the invoice id as `attachmentId` since invoices have no separate attachment table), enforces the admin+manager gate, returns the signed URL. Download button on the detail page calls the same route with `intent=download`. История на цените tab queries the last 5 prior `(vendorNameNormalized, descriptionNormalized)` matches per line item with a row-by-row delta column; no sparkline (charting dep not worth it for 5 data points). Delete action on the detail page reuses the existing `deleteInvoice` server action with a two-step destructive confirm; visible only to the uploader-while-pending or admins.

[12.05.2026] — Invoices module Round 2 shipped — upload + Claude-based PDF parser + preview modal. New dep: `@anthropic-ai/sdk` (~5 KB). Parser at `lib/invoices/parser.ts` uses Claude Sonnet 4.5 (`claude-sonnet-4-5`) with native PDF document blocks — no rasterisation needed. The prompt is in English (better instruction-following) but extracts Bulgarian fields. Source-currency detection happens in the model output; server-side conversion at the fixed peg (1 EUR = 1.95583 BGN) silently converts BGN amounts to EUR before the preview opens, so the user's "EUR only" directive holds even though all real supplier invoices are in BGN. Upload flow is three-phase: `parseAndStageInvoice` uploads to Supabase Storage + runs the parser + returns parsed data + duplicate warning; `confirmInvoice` writes Invoice + InvoiceLineItem rows; `discardStagedInvoice` cleans the Storage object on cancel. Storage path is `invoices/<section-slug>/<yyyy-mm>/<uuid>.pdf` (private bucket, signed URLs only). Duplicate check is soft (banner in modal, never blocks) on `(vendorNameNormalized, invoiceNumber, invoiceDate)`. Server Actions body-size limit bumped from 1 MB default → 12 MB in `next.config.ts` so 10 MB PDFs fit (with FormData overhead headroom). Validation runs both client-side (fast feedback) and server-side (defensive): vendor + number required, ISO dates, non-negative amounts, `total ≈ subtotal + vat ± 0.02 EUR`. Price-anomaly detection (`priceAnomalyPct` writes) deferred to Round 4; line items are inserted with null anomaly state in Round 2.

[12.05.2026] — Invoices module Round 1 shipped — schema + section admin + `/invoices` shell. New tables in the `public` schema: `InvoiceSection` (admin-configured upload buckets — slug is immutable since it's used in storage paths), `Invoice` (header-level record, EUR-only — no `currency` column, BGN-source invoices get converted at upload time in Round 2), `InvoiceLineItem` (with `priceAnomalyPct` + `priceAnomalyRefInvoiceId` fields ready for Round 4's detector). Both vendor and line-item description carry a denormalised `*Normalized` column populated server-side (`trim().toLowerCase().replace(/\s+/g, " ")`) — these power the price-history joins without needing a managed Vendor entity. Seed inserts 4 default sections (Офис / Строеж / Реновации / Архитектура) idempotently via raw SQL (`scripts/seed-invoice-sections.sql`). Admin CRUD lives at `/admin/invoice-sections` and mirrors `/admin/buildings` exactly — same patterns, same UX, same audit shape. `Фактури` nav link gated to admin+manager in `app/(app)/layout.tsx`; user role gets a 404 from the page itself per spec §11. Round 1 deliberately ships the upload button as a "Coming soon" toast — Round 2 wires the LLM parser + preview modal once the user drops sample PDFs in `files/invoices/samples/` (added to .gitignore).

[12.05.2026] — Calendar notes now render as inline spanning bars (monday.com / Google Calendar style) instead of hover-only tooltips. Both `/admin/calendar` (compact 12-month grid) and `/absence/calendar` (large month view) chunk their cells into per-week rows; for each week, consecutive days that share the same note text collapse into one horizontal bar spanning N grid columns. Bars sit above the day-number cells in row 1 of the week's mini-grid; cells occupy row 2. Same-note runs don't cross week boundaries — a 10-day note shows as two bars (one per week), matching how every standard calendar UI handles multi-day events. Admin grid: small warning-100 bar, `text-[10px]`, truncated; the corner-dot indicator we shipped earlier is removed (the bar makes it redundant). Team calendar: italic warning-toned bar; the per-cell repeated note block from the previous round is removed in favour of one bar per run (so a 5-day "Не сключвайте сделки" now reads as one continuous strip across the week, not 5 separate copies). Trigger: user feedback that hover-tooltips on `/admin/calendar` made notes invisible at a glance.

[12.05.2026] — `/admin/calendar` selection model reworked into direct-click UX. The "Масов режим" toggle is gone; clicking a day is now the only way to start an edit. First click sets a single-day selection (anchor); a second click on the same day confirms single, a second click on a different day forms a range. A third click restarts from a new anchor. The edit panel (day-type form + note form + clear-selection button) is sticky at the top of the calendar block and appears only when there's a selection — pre-fills the current override/note when a single day is selected, defaults to "holiday" for range. The `?edit=ISO` query-param path and the server-rendered single-day edit section in `page.tsx` are deleted; both forms in the panel call the bulk actions (which handle `start === end` as a 1-day range). Single-day server actions (`upsertCalendarDay`, `clearCalendarDay`, `upsertCalendarNote`) removed since the bulk versions subsume them. Trigger: user feedback that the two-step "toggle mode then click" pattern felt unnatural compared to clicking dates directly on the grid.

[12.05.2026] — Admin-set calendar notes shipped. New `CalendarNote` model in the `absence` schema (`day` PK = date, `note` text, created/updated timestamps) — deliberately **decoupled** from `CalendarDay` so disclaimers never influence `isWorking`, balance calcs, or absence-request validation. Admin UI: single-day edit form gets a "Бележка за деня" textarea (200-char cap, empty = delete); bulk panel now has two tabs — "Работни дни" (existing) and "Бележки" (apply or clear a note across the selected range, with the same skip-weekends option). Visual indicators: small accent-dot in the corner of each annotated cell on `/admin/calendar`, plus the existing hover-title that now combines holiday name and note. Team-facing surface: `/absence/calendar` renders the note inline inside each day cell in a warning-toned italic block, distinct from absence pills so it reads as an announcement, not as an absent person. Audit log gains `absence.calendar.note.update` + `absence.calendar.note.delete` actions; notes intentionally **ignore** the year-locked flag (informational, not historical record — admins should be able to fix a typo without unlocking a year). `/meetings/calendar` not wired in this round — its custom MonthView/WeekView components are a bigger ripple; deferred as a follow-up.

[12.05.2026] — Bulk range-edit shipped on `/admin/calendar`. New `<CalendarGrid>` client component (`app/(app)/admin/calendar/calendar-grid.tsx`) wraps the 12-month grid; a "Масов режим" toggle turns clicks into range selection (first click = start, second = end; clicking again restarts). Two date inputs mirror the selection so admins can type bounds directly. Apply panel chooses dayType (holiday / working / compensatory / clear-overrides) + optional holidayName + "skip weekends" checkbox. Server action `bulkUpsertCalendarDays` walks the range day-by-day in a single transaction, refuses any range that crosses a locked year, caps at 400 days, and still emits one audit row per day so the granular history matches the single-day path. Single-day edit flow (`?edit=ISO`) unchanged — bulk mode is purely additive. Cells are now `<button>` instead of `<Link>` (client navigates via `router.push` in non-bulk mode) which loses RSC prefetching on hover but the page is admin-only and already `force-dynamic`, so the trade is fine.

[11.05.2026] — Per-field help shipped (Phase 3 of navigation-help work). New `<FieldHelp>` component in `components/ui/field-help.tsx` — smaller sibling of `<PageHelp>` (16px circular `?` icon, 288px popover, click-to-open, portal-rendered, Escape-closes). Shared `<FormField>` extended with optional `help` / `helpTitle` props so every form built on it gets the affordance "for free". Wired only where the label alone isn't self-explanatory: ЕГН/ЕИК on contact form (10-digit personal vs 9-digit company); Тип on meetings (bulleted list of all 5 meeting types); Тип отсъствие on absence-submit (paid-balance vs sick-with-document vs unpaid semantics); Тип + Статус on property create-modal (13 property types incl. ПМ/ВПМ codes + 8 status enum); Системно име / Име за показване / Комплекс on /admin/buildings (immutable storage identifier vs free-rename display vs optional grouping). Inline-edit cells deliberately skipped — clicking into a cell already implies the field's meaning; column-header help is a future addition. Conventions + canonical copy logged in `bg-copy.md` §9d. Closes the three-phase navigation-help work (nav tooltips → page help → field help).

[30.04.2026] — Page-level help shipped (Phase 2 of navigation-help work). New `<PageHelp>` component in `components/ui/page-help.tsx`: small `?` icon next to H1, click-to-open popover with 2–4 sentences of usage instructions; closes on outside click, Escape, or explicit ✕; trigger has its own hover tooltip ("Помощ за тази страница") so the affordance is discoverable. Click rather than hover for richer multi-sentence content (mobile-friendly, more deliberate, doesn't fire accidentally). Wired into 12 pages — every top-nav surface plus inboxes (`/contacts`, `/properties`, `/leads`, `/leads/inbox`, `/meetings`, `/meetings/calendar`, `/tasks`, `/contracts`, `/absence`, `/absence/calendar`, `/absence/inbox`, `/team`). Detail/create/edit + admin pages deliberately skipped — different audience, more linear flow. Voice + length conventions for help bodies + canonical first-sentence flavors logged in `bg-copy.md` §9c. Phase 3 (per-field help) remains optional for ad-hoc rollout.

[30.04.2026] — Top-nav hover tooltips shipped (Phase 1 of the navigation-help work). New `<Tooltip>` primitive in `components/ui/tooltip.tsx`: portal-rendered, hover-with-delay (400ms default) + focus-immediate, Escape-closes, `aria-describedby` wired for screen readers, four-side positioning with `getBoundingClientRect`. Added `z-tooltip: 1300` to tailwind tokens (above toast). Wired to all 10 top-nav links with canonical Bulgarian copy logged in `bg-copy.md` §9b. Admin user-menu items deliberately skipped — labels are descriptive enough and hover tooltips inside a click-open dropdown fight the dismissal logic. Phase 2 (page-level `<PageHelp>` icons + per-page usage instructions) deferred to a follow-up — listed in roadmap.

[30.04.2026] — Final inline-edit primitives shipped — foundation now covers **10 of 12** spec field types. Three primitives + three real consumers in one round: `<InlineNumberCell>` (§3.6, locale-aware bg-BG parsing: accepts `12 500,50` + `12500.5`; rejects ambiguous `,`+`.` paste; three modes integer/decimal/currency-eur, optional suffix override) wired to `Meeting.durationMinutes` (integer, 0–720). `<InlineBooleanCell>` (§3.11, click-to-toggle, no popover, Space/Enter keyboard) wired to `Contract.usesCredit` (admin/manager gate, same `canEditStatus` flag). `<InlineMultiSelectCell>` (§3.3, checkbox-list popover with search-when-more-than-6, commit-on-close-or-Enter, Esc cancels, footer hint) wired to `Meeting.assignees` (many-to-many via MeetingAssignee join table; server validates ≥1 assignee, deactivated profiles rejected). Foundation now has primitives for: status, person, text, multiline, date, datetime, relation, number, boolean, multi-select. Remaining unbuilt: §3.10 (covered by relation-cell), §3.12 (covered by ReadOnlyBadge). Small-wins backlog effectively exhausted.

[30.04.2026] — Inline-edit rolled to `/meetings` with the new `<InlineDateTimeCell>` primitive (§3.8) — sixth+seventh foundation primitives (datetime + nothing else new this round). Fields wired: `startsAt`, `type`, `location`. `status` deliberately skipped — transitions go through dedicated `markHappened` + `cancelMeeting` actions that capture outcome/reason metadata, naive inline status would skip those. Per-row `canEdit` flag threaded from the page (computed via the now-shared `lib/meetings/permissions.ts` → `canEditMeeting()`, promoted from a private helper inside `[id]/actions.ts`); disables cells for non-assignees + cancelled meetings; server actions enforce same rule independently. Timezone handling exposed publicly: `sofiaWallClockToUtc` (already existed) + new `utcToSofiaWallClock` in `lib/meetings/parse.ts` — the cell speaks Sofia wall-clock strings, server converts at the seam. New audit action `meetings.field.updated` with `{ field, before, after }` payload, mirrors contacts/tasks pattern.

[30.04.2026] — Copy reconciliation pass landed alongside `bg-copy.md`: (1) `Опитай отново` → `Повтори` in `file-preview-modal.tsx` (line 392); also updated the two foundation specs (`ui-patterns-files.md`, `ui-patterns-inline-edit.md`) where the older string was quoted. (2) `Няма задачи в този изглед.` → `Няма намерени задачи.` in `app/(app)/tasks/{page,tasks-table}.tsx`. (3) `Без отговорник` → `— Без отговорник` in 4 places (contacts detail, tasks detail inline-cells, tasks-table, leads-table). The 5th hit in `app/(app)/leads/[id]/page.tsx` (warning StatusBadge for unassigned leads) kept as `Без отговорник` per a new spec exception in §6: the em-dash reads as noise inside a colored chip. Drift list in §15 cleared; section retained for future entries. Typecheck/lint/build all green.

[30.04.2026] — `specs/_foundations/bg-copy.md` shipped as first draft — canonical Bulgarian strings for buttons, confirmations, empty states, "no value" placeholders, validation errors, toasts, plurals (count form), locked-field tooltips, system attribution, address forms (formal "Вие" except destructive-confirm prompts). Surfaces three real drift items found by a 2-minute grep audit: (1) `Опитай отново` (file modal) vs `Повтори` (inline cells) — pick `Повтори`; (2) `Няма задачи в този изглед.` vs `Няма намерени X.` — unify to the latter; (3) `Без отговорник` vs `— Без отговорник` placeholder inconsistency. None blocking — listed in §15 for follow-up "copy reconciliation" PR. Spec is a writing guide + dictionary, not translation infrastructure; product is Bulgarian-only by design.

[30.04.2026] — Inline-edit treatment rolled to `/leads` (source) and `/tasks` (title + due-date). New server actions: `setLeadSource` (rejects email-sourced values per LEAD_SOURCE_USER_SELECTABLE; `email_form` / `email_unparsed` stay system-only set by the Resend webhook), `setTaskTitle` (required, max 200), `setTaskDueDate`. New audit actions: `leads.source.changed`, `tasks.field.updated` (granular field updates from inline cells). Promoted the previously-inlined `<ReadOnlyBadge>` to `components/ui/read-only-badge.tsx` so contacts/leads/tasks share one source. Spec wiring lists in §3.1 (status), §3.4 (text), §3.7 (date), §3.12 (read-only) updated. Properties + most of contracts deferred — properties needs its older one-off `<InlineCell>` migrated to the foundation primitives (separate refactor); contracts fields are mostly contract-locked per spec §9.

[30.04.2026] — Inline-edit foundation expanded with 4 new primitives — `<InlineTextCell>`, `<InlineMultilineCell>`, `<InlineDateCell>`, `<InlineRelationCell>` — bringing the foundation to 6 of the 12 spec field types built (status, person, text, multiline, date, relation). Wired into the `/contacts` table to make every field on the Contact model inline-editable: name, type, phone, email, ЕГН, address, properties, notes, birthDate, building, owner. Read-only fields (createdAt, age, birthdayThisYear) get a 🔒 hover badge per spec §3.12. New `contact.field.updated` audit action with `{ field, before, after }` payload (one entry per field change — keeps the audit log granular without bloating the AuditAction union with one action per field). Per-field server actions (`setContactName`, `setContactPhone`, ..., 9 total in `field-actions.ts`) — server-side validation (email regex, ЕГН via `lib/bg-id.ts`, length caps, building must be active). Validation behavior: invalid input rolls back with a Bulgarian toast; ЕГН checksum failure is non-blocking (per `specs/contacts.md` §6) but format/length errors reject. Date wire format is ISO YYYY-MM-DD; display uses `formatDate` (DD.MM.YYYY).

[30.04.2026] — Tasks module shipped end-to-end as a standalone module per `specs/tasks.md` (greenfield — spec written + code shipped together). `Task` Prisma model + `TaskStatus` enum (todo/in_progress/done). Three-tab list page (Мои / Всички / Завършени) with badge counts. Detail page + create + edit forms. Inline status + owner cells via the foundation primitives — proves the third independent consumer of `<InlineStatusCell>` and the third of `<InlinePersonCell>`. Permissions: open team-wide for create/edit/status/assign per user direction; admin-only delete. Setting status → done auto-stamps `completedAt`; moving back clears it. Overdue indication via left border on title cell when `dueDate < today` and status ≠ done. Deliberate non-features (out of v1, listed in spec §2): entity links, multi-assignee, priority, recurrence, email reminders, file attachments, activity feed. Each is a small additive change when demand appears.

[30.04.2026] — Admin delete UI for contracts attachments wired into the file preview modal. Two-step confirm pattern (Click 🗑 → toolbar swaps to a danger-tone confirm row with [Изтрий завинаги] [Отказ] buttons; auto-resets after 5s). The `<FilePreviewModal>` and `<FileCell>` got an optional `onDelete` prop — when undefined, no delete affordance renders. Per spec §8.1 deletion is admin-only, so `canDeleteAttachments = me.role === "admin"` is computed once in the page and threaded to the cell. Both the `/contracts` table and the `/contracts/[id]` Files section get the same delete capability via the shared `<ContractFiles>` wrapper. Rebuild confirmed: typecheck/lint/build all green.

[30.04.2026] — `<InlineStatusCell>` primitive shipped (`components/ui/inline-status-cell.tsx`), generic over the enum value type with optional `systemOnly` flag for picker-hidden values (e.g. Lead.status `converted` is set by the Contracts conversion flow, not user-pickable). Wired into `/leads` (all roles can edit) and `/contracts` (admin/manager only — the cell is `disabled` for users via a `canEditStatus` flag threaded from the page; the action also requires the role server-side). New actions `setLeadStatus`, `setContractStatus`, `setPropertyStatus`. Visual: each option in the popover renders with the same `<StatusBadge>` tone classes as the trigger so the popover reads as "buttons that *are* the badge". Search input only renders when there are >4 options (contracts has 3 → no search; properties will have 8 → search). Properties table NOT migrated this round — it has an older one-off `<InlineCell>` predating the foundation that handles status + type + seller; migration is a separate refactor. The `setPropertyStatus` action is staged for that migration. The `+ Нов статус` admin-inline-create button (spec §3.1) is deferred — adding values to a string enum is schema work.

[30.04.2026] — `applyCarryover` extended with a backward pass (`computeCarryover` exposes both the per-payment view + final unused credit). Forward pass alone left the per-payment "Остава" view summing to a different number than the contract-level total when an overpayment landed on a milestone whose subsequent dues were all 0 — the unused credit just sat in the chain, never consumed. Backward pass walks the per-payment list oldest-first and consumes any final leftover credit against earlier `adjustedRemainingEur` values, so sum-of-payments now matches the contract net. Detail page shows "Покрито от бъдещо надплащане" badge per payment when this kicked in, plus a "Свободен кредит" footer when the contract is net-overpaid (rare but real — e.g. fee refunds).

[30.04.2026] — Inline owner edit ported to `/leads` (`InlinePersonCell` + `setLeadOwner` server action). Mirror of the contacts pattern; same permission model (any signed-in profile can reassign). The `leads.owner.changed` audit action already existed from the original leads ship — just a new emission point. Confirms the primitive's reusability claim; same-shape callsite, different module.

[30.04.2026] — Contracts attachments wired up end-to-end (first production consumer of the file-cell pattern). Added `ContractAttachment` Prisma model, extended `lib/supabase/storage.ts` with `uploadFile` / `deleteFile` / `ensureBucketExists` (auto-creates the `contracts` bucket private, 25 MB cap on first call). Server actions `uploadContractAttachment` / `deleteContractAttachment` (delete is admin-only per spec §9). `<FileCell>` got a "+" upload button trailing the icon stack. Detail page `/contracts/[id]` now has a "Файлове" section rendering the same `<FileCell>` — table and detail share the data via revalidatePath, so uploads propagate automatically. Demo wiring (`lib/files/demo.ts`, `contracts.demo` switch case, `/public/demo/*`) deleted. Foundation spec §2 updated: upload UX is now in scope for the table cell (modules opt out via not passing `onUpload`).

[30.04.2026] — Cross-module file-attachment pattern shipped as a foundation (`specs/_foundations/ui-patterns-files.md`): `<FileCell>` icon stack inline, `<FilePreviewModal>` modal viewer, `POST /api/files/sign` as the single signing endpoint with per-module dispatch. First Supabase Storage usage (`lib/supabase/storage.ts`) — service-role client, 5-min signed URLs, silent re-fetch 30s before expiry. Modal renders PDFs via native `<iframe>` (no PDF.js dep) and images via `<img>`; non-previewable types fall back to a download CTA. Demo wiring in `lib/files/demo.ts` + `module: "contracts.demo"` switch case is throwaway — remove when contracts attachments ship. Why a generic primitive: contracts and invoices both already design `*Attachment` tables in their specs, and shipping a one-off cell per module would diverge.

[30.04.2026] — First inline-edit cell shipped: owner column on `/contacts` (see `components/ui/inline-person-cell.tsx`, `app/(app)/contacts/owner-actions.ts`). Built as a reusable primitive — leads/tasks/etc. will share it. Implements the canonical pattern from `_foundations/ui-patterns-inline-edit.md` §3.9 (popover, search-as-you-type, ↑/↓ + Enter, "— Без отговорник" first, full optimistic with rollback). Also added: minimal `<ToastProvider>` (no library — kept dep-free per CLAUDE.md), `<AvatarCircle>` initials primitive (deterministic palette over existing tokens), `contact.owner.changed` audit action. Permissions: matches spec — any signed-in profile can reassign (no role gate beyond `requireProfile`). Popover uses `position: fixed` with computed coords because the Table wrapper has `overflow-hidden`. Inactive owners stay rendered as the current value with a `(неактивен)` tag but never appear in the picker list.

[27.04.2026] — Admin calendar edit form replaces the old "Работен ден" checkbox + free-text name with a 3-option select (`holiday` / `working` / `compensatory`); the checkbox + name combo silently allowed `isWorking=true, holidayName="X"` ghost rows that didn't render anywhere (caught when 6 May 2026 / Гергьовден was marked but invisible). The select makes intent unambiguous; `isAnnotatedOnly` paint (warning-50 ring) on the grid surfaces any pre-existing ghost rows so admins can re-edit them. Also fixed `yearStart`/`yearEnd` bounds: were noon UTC, missed Jan 1 overrides stored at midnight (see `app/(app)/admin/calendar/{page,actions}.tsx`).

[24.04.2026] — Spec folder restructured: `docs/` → `specs/` with `_foundations/`, `design-system/`, top-level lowercase module files — matches `CLAUDE.md` + `_index.md`; every cross-reference now resolves.

[24.04.2026] — LP2-C (Resend inbound webhook) wired up. Resend's `email.received` payload is metadata-only; raw MIME must be fetched via `GET /emails/receiving/{email_id}` using `data.email_id` from the webhook, then the signed `raw.download_url` from that response (see `app/api/leads/ingest/route.ts`'s `fetchResendRawMime`). Required a **full-access** API key; a restricted/sending-only key returns empty lists on `/emails/receiving` (silent — no 401/403). Store the key in `RESEND_API_KEY` — same var used for outbound.

[23.04.2026] — `Property.owner` unlocked for manual editing in Phase 1 until Contracts ships — otherwise there's no way to express ownership at all; re-locks automatically once a `contractId` is set (see `lib/properties/permissions.ts`).

[23.04.2026] — Contact typeahead search excludes the `notes` column — notes carry relationship/alias text that surfaced contacts for non-obvious reasons when searching for owners (see `lib/contacts/search.ts`).

[23.04.2026] — Contact typeahead result limit is 25 (up from 10), with a "Показани първите 25" footer when the list is maxed out so users know more matches may exist.

[22.04.2026] — Properties CSV seed is Windows-1251, not UTF-8 — verified by reading raw bytes (`D1 E3 F0 E0 E4 E0` = `Сграда` in CP-1251). The import script uses `TextDecoder("windows-1251")`; reading as UTF-8 produces mojibake and collapses many buildings into the same string (see `scripts/import-properties.ts`, `specs/properties.md` §7.1).

[22.04.2026] — `landPct` / `yardPct` column precision is `decimal(10, 6)` not `(8, 6)` — one CSV row has `landPct = 203` (data error); bumping precision lets the import complete rather than reject a single outlier (see `prisma/schema.prisma`).

[22.04.2026] — The 2,158-row CSV has 311 duplicate `(Сграда, Name)` pairs → only 1,847 unique properties after import. `(buildingId, name)` is the natural key; second occurrence wins (see `specs/properties.md` §7.3).
